## 说明
通过定界符提取名称

## 用法

**`pip install delimiter_name_extract`**

mode='all' = 提取定界符内全部字符

mode='num' = 提取定界符内数字

```python
delimiter_name_extract(filename, delimiters=['[]', '【】'], mode='all'):
```
