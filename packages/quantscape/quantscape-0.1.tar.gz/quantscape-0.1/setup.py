from setuptools import setup, find_packages

setup(
    name="quantscape",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # list your dependencies here (if any)
    ],
)

