"""
In Py2, strings are ASCII by default, means unicode and str are different, but bytes and str are same
In PY3, strings are unicide by default, means unicode and str are same, but bytes and str are different

"""