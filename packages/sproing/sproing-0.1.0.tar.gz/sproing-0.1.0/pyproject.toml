[tool.poetry]
name = "sproing"
version = "0.1.0"
description = ""
authors = ["danodic <danilo@danodic.dev>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pytest = "*"

[tool.poetry.dev-dependencies]
mypy = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
