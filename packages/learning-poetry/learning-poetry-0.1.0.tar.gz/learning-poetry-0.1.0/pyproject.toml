[tool.poetry]
name = "learning-poetry"
version = "0.1.0"
description = ""
authors = ["Pető Tamás <petotamas0@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
idna = "^3.4"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
