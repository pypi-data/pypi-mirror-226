from setuptools import setup, find_packages

setup(
    name='pyshode',
    version='1.0',
    packages=find_packages(),
    author='akamedagoat',
    description='A python module to custom terminal.',
)