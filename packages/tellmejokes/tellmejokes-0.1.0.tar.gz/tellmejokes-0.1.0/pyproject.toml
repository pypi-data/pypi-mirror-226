[tool.poetry]
name = "tellmejokes"
version = "0.1.0"
description = "jokes cli app using APIs"
authors = ["Ejaz"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.7"
requests = "^2.31.0"
twine = "^4.0.2"


[tool.poetry.group.dev.dependencies]
black = "^23.7.0"
flake8 = "^6.1.0"
isort = "^5.12.0"
twine = "^4.0.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
jokes = "tellmejokes:jokes"
