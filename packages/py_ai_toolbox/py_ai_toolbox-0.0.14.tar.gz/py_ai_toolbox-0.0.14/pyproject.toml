[build-system]
requires = [ "flit_core >=3.2,<4",]
build-backend = "flit_core.buildapi"

[project]
name = "py_ai_toolbox"
version = "0.0.14"
authors = []
classifiers = [ "License :: OSI Approved :: MIT License",]
dynamic = [ "description",]

[project.license]
file = "LICENSE"

[project.scripts]
ait-hf-download = "ai_toolbox.hf_download:run"
