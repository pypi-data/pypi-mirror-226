from setuptools import setup, find_packages

setup(
    name='SyntaxLib',
    version='0.1',
    packages=find_packages(),
    install_requires=[]
        # Any dependencies your package needs
    
)
