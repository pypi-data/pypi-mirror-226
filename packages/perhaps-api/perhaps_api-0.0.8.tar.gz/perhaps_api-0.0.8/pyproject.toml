[tool.poetry]
name = "perhaps-api"
version = "0.0.8"
description = ""
readme = "README.md"
authors = []
packages = [
    { include = "perhaps", from = "src"}
]

[tool.poetry.dependencies]
python = "^3.7"
httpx = "0.23.3"

[tool.poetry.dev-dependencies]
mypy = "0.971"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
