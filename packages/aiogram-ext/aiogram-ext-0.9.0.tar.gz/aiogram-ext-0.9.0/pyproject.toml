[project]
name = "aiogram-ext"
version = "0.9.0"
description = ""
authors = []
dependencies = [
    "motor>=3.1.2",
    "parsenv>=0.2.2",
    "setuptools>=67.7.2",
    "aiogram>=2.25.1",
]
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-pep517>=1.0",
]
build-backend = "pdm.pep517.api"

[tool.pdm.scripts]
test = "python tests"

[tool.pdm.scripts._]
env_file = ".env"

[tool.pdm.scripts._.env]
PYTHONPATH = "."
